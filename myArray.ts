const superHero: string [] = []
const superHeros: number[] = []

type User = {
    name:string;
    isActive:boolean
}

const allUser : User []=[]

allUser.push({name:"", isActive : true})
superHero.push("mukesh")